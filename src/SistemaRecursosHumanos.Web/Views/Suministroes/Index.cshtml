@model IEnumerable<SistemaRecursosHumanos.Core.Domain.Suministro>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreSuministro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSolicitudNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreSuministro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSolicitudNavigation.IdSolicitud)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSuministro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSuministro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSuministro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
